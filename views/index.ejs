<% layout('base') %>

<div class="index">
    <div class="login">
        <div class="header">System Login</div>
        <div class="infos">
            <form method="post" action="/login">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="field">
                    <label for="account"><span class="material-icons">person</span></label>
                    <input class="textfield" autocomplete="true" placeholder="Username" id="account" name="account" type="text" />
                </div>
                <div class="field">
                    <label for="password"><span class="material-icons">password</span></label>
                    <input class="textfield" autocomplete="true" placeholder="Password" id="password" name="password" type="password" />
                </div>
                <div class="btn">
                    <label for="submit"><span class="material-icons">login</span></label>
                    <input id="submit" type="submit" value="Login" />
                </div>
            </form>
        </div>
    </div>
</div>